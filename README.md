helps open-source beginners find issues they can actually pick up. It searches GitHub for issues labeled good first issue or help wanted, drops stale and already-claimed ones, and shows the rest in a clean list with filters for language, label, and how recent they are. It's built on the Next.js App Router with server-side data fetching, cached GitHub API calls, and filters stored in the URL so any search can be shared as a link.
